How to run code?
Save the files in a folder
Open that folder in terminal
Type javac *.java to compile the files
Type java Main to run the code

NOTE: the Main class already contains pre-feeded data, which can also be changed, if required for evaluation of the functionality of code
